# 社区插件审核与事件处理

维护者：NoteGen。状态：流程草案，尚未开放投稿。更新：2026-09-09。

## 开放条件

先完成官方插件的生产安装、更新、撤销、换钥和恢复验收，确认维护者能处理签名失败、索引过期和主源故障，再公告开放社区投稿。提交 PR 不代表自动上架。

## 每次审核留档

| 项目 | 作者提供 | 维护者确认 |
| --- | --- | --- |
| 来源 | 源码仓库、许可证、完整提交 SHA、联系方式 | 发布者身份与源码所有权 |
| 可复现性 | SDK 版本、lockfile、构建命令、产物 SHA-256 | 从固定提交重建并核对内容 |
| 权限 | 每项权限的用途和最小目录/域名范围 | 不存在未说明的数据上传或额外权限 |
| 行为 | 主要操作、失败路径、卸载和数据迁移说明 | 在真实桌面宿主执行安装、授权、操作、更新和回滚 |
| 签名 | 发布者公钥与 keyId | 经审核登记；私钥不进入 PR、Issue 或日志 |
| 兼容 | 最低宿主/API 版本、系统支持范围 | 使用所声明的宿主版本验证，不以模拟宿主代替 |
| 用户资料 | 离线 USAGE、截图、变更说明 | 描述与实际能力一致 |

维护者将审核提交、SDK 提交、包摘要、审核人、日期和验收记录放入发布记录。社区源码留在作者仓库。市场工具目前以官方插件登记为入口；正式开放前还需实现接收外部不可变包及其独立发布者登记的发布路径，不要把社区包冒充官方构建发布。

## 安全事件

1. 记录受影响插件 ID、版本、包摘要和来源，暂停问题版本发布。
2. 在登记的 `revocations` 中列出受影响版本及可公开的原因，发布更高代际签名索引。
3. 在已安装问题版本的客户端刷新市场，确认停止运行、重启不加载、不能回滚到问题版本。
4. 密钥泄露时更换发布者密钥，以 `previousPublisherKeys` 维持可审核的更新路径；同时撤销受影响旧版本。保留旧包摘要作为调查证据。
5. 发布修复版本及数据恢复说明。离线客户端收到索引前无法被远程停止，不应宣称即时全量撤销。

## 所有权转移与停止维护

相同发布者 ID 的正常换钥由根签名索引登记旧公钥授权。跨发布者所有权转移暂不自动接受：新发布者使用新的插件 ID，用户手动安装和授权；不得静默继承旧授权或覆盖旧包。

作者停止维护时保留最后兼容版本与许可证信息，取消推荐并说明状态。仅存在安全问题时撤销运行版本；不要通过撤销正常插件强迫用户升级。
